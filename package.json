{
  "name": "home-assistant-plugin",
  "version": "1.0.0",
  "description": "Home Assistant Plugin for ChatGPT",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.26.1"
  }
}
